====================================
Git使用.gitignore过滤上传文件
====================================

很多时候我们并不想仓库中所有文件都要推送到远程仓库中，此时可以在仓库的根目录上创建一个.gitignore文件。

记事本打开写入如下内容：

.. code-block:: python

    *.py[cod]
    .vscode
    *.nk.autosave
    *.nk~
